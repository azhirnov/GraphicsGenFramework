
#import  "Compute/SimpleParticles/shared.glcs"

DECL_BUFFER( std140, Particle, readonly,  inBuffer );
DECL_BUFFER( std140, Particle, writeonly, outBuffer );

uniform float unTimeDelta;
uniform float unGlobalTime;


void main ()
{
	int		index = GetGlobalIndex();

	UpdateParticle( inBuffer[index], OUT outBuffer[index], unTimeDelta, unGlobalTime, 0.001 );
}
